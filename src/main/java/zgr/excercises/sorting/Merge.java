package zgr.excercises.sorting;

/**
 * Conceptually, a merge sort works as follows:
 * Divide the unsorted list into n sub-lists, each containing one element (a list of one element is considered sorted).
 * Repeatedly merge sub-lists to produce new sorted sub-lists until there is only one sublist remaining. This will be
 * the sorted list.
 */
public class Merge {

    private Merge() {}

    public static int[] sort(int[] input) {
        // TODO implement logic here
        return input;
    }
}
